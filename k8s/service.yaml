apiVersion: v1
kind: Service
metadata:
  name: nlp-app-service
  namespace: devsecops
  labels:
    app: nlp-app
    app.kubernetes.io/name: nlp-app
spec:
  type: ClusterIP
  selector:
    app: nlp-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
